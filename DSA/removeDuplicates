def removeDuplicates(nums):
        dict={}
        for i in range(0,len(nums)):
            dict[nums[i]]=0
        
        j=0
        for k in dict:
            nums[j]=k
            j+=1
        print(j, nums[:j])

nums=[1,1,2,3,3,4,4]
removeDuplicates(nums)